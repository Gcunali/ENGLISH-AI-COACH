import { BookOpenCheck,Check,Clock3,Headphones,History,LockKeyhole } from 'lucide-react'
import { useEffect,useState } from 'react'
import { useNavigate } from 'react-router-dom'
import { ErrorState,LoadingState } from '../components/PageState'
import { AppCard,InlineNotice,PageHero,PageShell,SectionHeader } from '../components/ProductUI'
import { getToeicOverview,getToeicPart2Overview,getToeicPart3Overview,getToeicPart4Overview,startToeicPart2Session,startToeicPart3Session,startToeicPart4Session,startToeicSession } from '../services/native'
import type { ToeicOverview,ToeicPart2Overview,ToeicPart3Overview,ToeicPart4Overview } from '../types'
export function ToeicPage(){const nav=useNavigate();const[p1,setP1]=useState<ToeicOverview|null>(null);const[p2,setP2]=useState<ToeicPart2Overview|null>(null);const[p3,setP3]=useState<ToeicPart3Overview|null>(null);const[p4,setP4]=useState<ToeicPart4Overview|null>(null);const[error,setError]=useState<string|null>(null);const[busy,setBusy]=useState<string|null>(null);const load=()=>{setError(null);void Promise.all([getToeicOverview(),getToeicPart2Overview(),getToeicPart3Overview(),getToeicPart4Overview()]).then(([a,b,c,d])=>{setP1(a);setP2(b);setP3(c);setP4(d)}).catch(x=>setError(msg(x)))};useEffect(load,[]);if(error&&!p1)return <ErrorState message={error} onRetry={load}/>;if(!p1||!p2||!p3||!p4)return <LoadingState label="Loading the local TOEIC item bank…"/>;const start=async(id:string,v:number,part:number)=>{setBusy(id);try{if(part===4){const s=await startToeicPart4Session(id,v);nav(`/toeic/part4/session/${s.sessionId}`)}else if(part===3){const s=await startToeicPart3Session(id,v);nav(`/toeic/part3/session/${s.sessionId}`)}else if(part===2){const s=await startToeicPart2Session(id,v);nav(`/toeic/part2/session/${s.sessionId}`)}else{const s=await startToeicSession(id,v);nav(`/toeic/session/${s.sessionId}`)}}catch(x){setError(msg(x))}finally{setBusy(null)}};return <PageShell width="wide"><PageHero eyebrow="Exam preparation" title="TOEIC Preparation" accent="Listening & Reading" description="Offline TOEIC-style practice with deterministic grading. All four Listening parts are available offline." compact/>{error&&<InlineNotice tone="warning" live>{error}</InlineNotice>}<div className="toeic-trust-row"><span><Clock3/>Untimed by design</span><span><LockKeyhole/>Answers stored locally</span><span><BookOpenCheck/>Authored explanations</span></div><Forms title="Listening Part 1 — Photographs" description="Look at each photograph and choose the best spoken description." forms={p1.forms} total={6} busy={busy} open={(id,v,a)=>a?nav(`/toeic/session/${a}`):void start(id,v,1)}/><Forms title="Listening Part 2 — Question–Response" description="Listen to a prompt and three responses, then choose the best response." forms={p2.forms} total={25} busy={busy} open={(id,v,a)=>a?nav(`/toeic/part2/session/${a}`):void start(id,v,2)}/><Forms title="Listening Part 3 — Conversations" description="Read three questions, listen once to a two-speaker conversation, and lock all answers before feedback." forms={p3.forms} total={39} busy={busy} open={(id,v,a)=>a?nav(`/toeic/part3/session/${a}`):void start(id,v,3)}/><AppCard className="mt-4"><div className="section-header"><div><p className="eyebrow">Full exam architecture</p><h2 className="section-title">Listening & Reading · 200 questions</h2><p className="section-description">Listening Parts 1–3 are available. Part 4 and Reading remain unavailable.</p></div><div className="toeic-result-actions"><button className="button-primary" onClick={()=>nav('/toeic/listening')}><Clock3/>Full Listening Simulation</button><button className="button-secondary" onClick={()=>nav('/toeic/history')}><History/>TOEIC Performance</button></div></div><div className="toeic-parts-grid">{p1.parts.map((part,i)=>{const available=part.runtimeAvailable||part.part==='part2_question_response'||part.part==='part3_conversation'||part.part==='part4_talk';return <div className={`toeic-part-row ${available?'available':''}`} key={part.part}><span>{available?<Check/>:<LockKeyhole/>}</span><div><strong>Part {i+1} — {part.title}</strong><small>{part.questionCount} questions</small></div><b>{available?'Available':'Not yet available'}</b></div>})}</div></AppCard><p className="toeic-disclaimer">{p1.disclaimer}</p></PageShell>}
function Forms({title,description,forms,total,busy,open}:{title:string;description:string;forms:Array<{formId:string;formVersion:number;title:string;activeSessionId:string|null}>;total:number;busy:string|null;open:(id:string,v:number,a:string|null)=>void}){return <AppCard className="mt-4"><SectionHeader title={title} description={description}/><div className="toeic-form-grid">{forms.map(f=><article className="toeic-form-card" key={f.formId}><div className="toeic-part-icon"><Headphones/></div><div><p className="eyebrow">{total} questions · Untimed</p><h3>{f.title}</h3><p>Original local content with deterministic scoring and explanations.</p></div><button className="button-primary" disabled={busy!==null} onClick={()=>open(f.formId,f.formVersion,f.activeSessionId)}>{f.activeSessionId?'Resume':busy===f.formId?'Starting…':'Start form'}</button></article>)}</div></AppCard>}function msg(x:unknown){return x instanceof Error?x.message:String(x)}


