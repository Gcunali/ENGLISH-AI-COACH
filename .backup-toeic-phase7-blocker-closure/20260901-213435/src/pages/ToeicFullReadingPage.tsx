import {useEffect,useState} from 'react';import{BookOpenCheck,ChevronRight,Pause}from'lucide-react';import{useNavigate,useParams}from'react-router-dom';import{AppCard,InlineNotice,PageHeader,PageShell}from'../components/ProductUI';import{ErrorState,LoadingState}from'../components/PageState';import{getToeicFullReading,startToeicFullReading}from'../services/native';import type{ToeicFullReadingSession}from'../types';
export function ToeicFullReadingPage(){const{id}=useParams();const nav=useNavigate();const[data,setData]=useState<ToeicFullReadingSession|null>(null);const[error,setError]=useState<string|null>(null);const load=()=>{setError(null);void(id?getToeicFullReading(id):startToeicFullReading('simulation')).then(s=>{setData(s);if(!id)nav(`/toeic/reading/${s.sessionId}`,{replace:true})}).catch(x=>setError(x instanceof Error?x.message:String(x)))};useEffect(load,[id]);if(error)return <ErrorState message={error} onRetry={load}/>;if(!data)return <LoadingState label="Restoring Full Reading…"/>;const current=data.parts.find(p=>p.status!=='completed');return <PageShell width="wide"><PageHeader eyebrow="Untimed TOEIC-style Reading Simulation" title={data.status==='completed'?'Reading complete':'Full Reading · 100 questions'} description={`${data.answeredCount}/100 first answers locked · Simulation Mode · Pause anytime`}/><InlineNotice tone="info">No correctness, answer keys, completed passages or evidence are shown until all 100 questions are complete.</InlineNotice>{data.status==='completed'&&data.estimate?<AppCard><h2>Estimated Reading: {data.estimate.estimatedScore}</h2><p>Likely practice range: {data.estimate.rangeLow}–{data.estimate.rangeHigh}</p><p>{data.disclaimer}</p></AppCard>:<AppCard><h2>{current?`Part ${current.partNumber} — ${current.title}`:'Preparing result'}</h2><div className="toeic-parts-grid">{data.parts.map(p=><div className={`toeic-part-row ${p.status==='completed'?'available':''}`} key={p.partNumber}><BookOpenCheck/><div><strong>Part {p.partNumber} — {p.title}</strong><small>{p.questionCount} questions · {p.status}</small></div></div>)}</div>{current&&<button className="button-primary mt-3" onClick={()=>nav(current.route)}>Continue Part {current.partNumber}<ChevronRight/></button>}</AppCard>}<button className="button-ghost mt-3" onClick={()=>nav('/toeic')}><Pause/>Pause & exit</button></PageShell>}
