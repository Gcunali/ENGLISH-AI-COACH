# Reconstructed pre-UI-fix baseline

These two files reconstruct the exact Phase W state immediately before the small Phase X installed-range notice and its test. They were reconstructed after the later content-driven UI issue was discovered; they are explicitly not represented as timestamped pre-change captures. All initially anticipated Rust/manifests/database files were physically copied before Phase X changes.
